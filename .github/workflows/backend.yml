name: 'Бэкенд CI'

on:
  push:
    branches: [master, backend]
    paths: ['backend/**']
  pull_request:
    paths: ['backend/**']

concurrency:
  group: backend-${{ github.ref }}
  cancel-in-progress: true

# TODO: добавить jobs когда бэкенд будет готов
jobs:
  placeholder:
    name: Бэкенд (заглушка)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Бэкенд CI пока не настроен"
