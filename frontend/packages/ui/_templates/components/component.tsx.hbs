import type { IBaseWithChildrenProps } from "@/types";
import type { FC } from "react";
import clsx from "clsx";
import Styles from './{{kebabCase component}}.module.css';

export type {{pascalCase component}}Props = IBaseWithChildrenProps & {};

export const {{pascalCase component}}: FC<{{pascalCase component}}Props> = ({
  className,
  style,
}) => {
  return (
    <div className={clsx(Styles.container, className)} style={style}></div>
  );
};
